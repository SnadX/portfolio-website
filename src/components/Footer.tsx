import React from 'react';

const Footer: React.FC = () => {
    return (
        <footer>
                <p>&copy; {new Date().getFullYear()} Jacky Wu </p>
        </footer>
    );
};

export default Footer;